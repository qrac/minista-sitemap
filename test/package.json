{
  "name": "minista-sitemap-test",
  "private": true,
  "scripts": {
    "dev": "minista",
    "build": "minista build",
    "clean": "trash ./dist"
  },
  "devDependencies": {
    "minista": "^0.8.3"
  }
}
